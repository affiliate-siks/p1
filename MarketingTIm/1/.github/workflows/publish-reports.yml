name: Publish Reports

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Confirm workflow is working
        run: echo "Workflow radi ispravno"
